import { defineConfig } from "vite";

export default defineConfig({
  base: '/codegrid-sticky-cards-ashfall-rebuild-js/',
  // publicDir: "public",
  // build: {
  //   assetsInlineLimit: 0,
  // },
  // server: {
  //   open: true,
  // },
});
