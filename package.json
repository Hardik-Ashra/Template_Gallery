{
  "name": "template-gallery",
  "private": true,
  "workspaces": [
    "apps/*",
    "server"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build ",
    "start": "node server/generateTemplatesJson.js && node server/index.js",
    "generate-templates-json": ""
  },
  "devDependencies": {
    "turbo": "^1.12.3"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
